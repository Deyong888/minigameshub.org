---
import Layout from '~/layouts/PageLayout.astro';
import GameGrid from '~/components/widgets/GameGrid.astro';
import { arcadeModernGames } from '~/data/games';

const metadata = {
  title: 'Modern Arcade Games - New Age Fun',
  description: 'The spirit of arcade in modern games. High quality graphics and addictive gameplay.',
};

const schema = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": "Modern Arcade Games",
  "description": metadata.description,
  "url": "https://minigameshub.org/arcade-games/modern",
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": arcadeModernGames.map((game, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "item": {
        "@type": "VideoGame",
        "name": game.title,
        "url": `https://minigameshub.org/game/${game.slug}`,
        "image": game.thumbnail,
        "description": game.description,
        "genre": game.category,
        "playMode": "SinglePlayer",
        "applicationCategory": "Game"
      }
    }))
  }
};
---

<Layout metadata={metadata}>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
  <section class="px-4 py-16 sm:px-6 mx-auto lg:px-8 lg:py-20 max-w-4xl">
    <h1 class="font-bold font-heading text-4xl md:text-5xl leading-tighter tracking-tighter mb-4 font-heading text-center">
      Modern Arcade Games
    </h1>
    <p class="text-xl text-muted mb-8 text-center">
      Arcade spirit with modern technology.
    </p>
    <GameGrid games={arcadeModernGames} />
  </section>
</Layout>
